# 思源笔记 Mermaid 双向可视化编辑插件 — 架构设计文档

| 版本 | 说明 | 日期 |
| --- | --- | --- |
| v1.0 | 初版架构设计（方案A：Visimer/CST 为主的技术选型） | 2026-09-16 |

---

## 1. 需求背景与问题陈述

思源笔记（SiYuan）是一款基于 Electron / 纯前端技术栈、底层核心为 ProseMirror 深度定制块级（Block）富文本/Markdown 编辑器的笔记软件。在思源中，Mermaid 图表本质上是一个 **code-block（代码块）**，用户只能直接编辑其中的 Mermaid 纯文本。

### 1.1 现状痛点

- 用户在思源中用纯文本维护 Mermaid 语法，无法像专业绘图工具那样 **可视化拖拽节点、连线、双击改字**。
- 语法一旦写错，只能在 ```` ```mermaid ```` 代码块里逐字排查，缺乏即时可视化反馈。
- 世间缺乏 **既贴合思源块级架构、又不破坏原生 Markdown 存储** 的双向 WYSIWYG 编辑方案。

### 1.2 需求目标

构建一款思源插件，为 Mermaid 代码块提供 **双向（Bidirectional）WYSIWYG 可视化编辑**，实现：

> 用户在可视化画布上增删节点、拖拽连线、双击改字后，**无损且实时**地写回思源代码块中的 Mermaid 纯文本；反之，修改代码块文本，画布同步重渲染。

核心约束：**100% 兼容思源的原生 Markdown 存储**——即使插件被卸载，笔记内容也完全不会损坏。

---

## 2. 目标与非目标

### 2.1 目标（In-Scope）

- 插件以 **TypeScript** 构建，打包为思源可加载的标准插件（vite + esbuild + plugin.json）。
- 在 Mermaid 代码块的 **block-icon / 快捷键** 触发可视化编辑，弹出思源标准 **Dialog** 承载画布。
- 正向数据流：`block.getBlockMarkdown` → 提取 Mermaid 纯文本 → Visimer（CST）渲染可编辑画布。
- 反向数据流：画布拖拽/改字 → CST 修改原文本 → `block.updateBlock` 写回（附防抖策略）。
- 首期正式支持 **flowchart** 图类型。
- 对 sequence / class / gantt 等其它图类型提供 **降级策略**（纯文本 / 只读预览）。
- 设计一层 **类型适配器 + 能力探测** 机制，支撑 Visimer 未来迭代升级新增图类型时的平滑接入。

### 2.2 非目标（Out-of-Scope）

- 不重写 / 替换思源自带的 Mermaid 渲染引擎（避免思源核心升级时插件崩溃）。
- 不做 Draw.io 类巨型白板（太重，无法嵌入思源 Block 块）。
- 不做多端离线协同下的写冲突复杂合并。
- 本期不实现其它图类型的可视化编辑（仅做降级与预留扩展）。

---

## 3. 技术选型：方案对比与结论

### 3.1 候选方案对比

| 维度 | 方案A：Visimer / CST | 方案B：React Flow / Vue Flow 桥接 |
| --- | --- | --- |
| **核心思路** | mermaid-wysiwyg（CST：Concrete Syntax Tree）直接操作原文本字符串 | 官方 `@mermaid-js/parser` 解析成 AST → 转 nodes/edges → 画布渲染，自写 ast→mermaid 生成器 |
| **开发速度** | 快，开箱即用 | 慢，需自写转换器 |
| **Mermaid 原生保真** | 高，语义模型无失真 | 中，依赖转换器完整性 |
| **复杂语法 / 极端子图** | 有边缘 Bug 风险 | 稳定 |
| **超大图（成百上千节点）** | 一般 | 优（缩放/多选/网格吸附成熟） |
| **对思源 Block 契合度** | 极高（纯文本往返，不破坏结构） | 高（需中间层转换） |

### 3.2 选型结论

依据 need.md 与技术背景，本项目主线采用 **方案A：Visimer / CST**。

理由：

1. **原生保真与存储安全**：方案A 完全不改变底层数据结构，文本往返零失真，是最契合思源 code-block 的"轻量化桥接结构"。
2. **开发效率高**：插件以"微调代码块"为核心场景，方案A 即为此而生，与思源契合度极高。
3. **风险可控**：方案B 的"绝不能出 Bug"诉求不构成本项目的首要矛盾；首期定位 flowchart，方案A 对该类型成熟度足够。

> **兼容性兜底**：在设计上保留方案B（vue/React-Flow 桥接）作为可替换的**渲染后端**（见 §8 适配器），一旦 Visimer 在后续迭代中对某类型持续暴露 Bug，可无侵入切换渲染后端而**不动双向绑定核心逻辑**。

---

## 4. 总体架构

插件采用 **三层架构 + 一个适配层**：

```
┌─────────────────────────────────────────────────────────────┐
│                     思源笔记 Host（SiYuan）                  │
│   ProseMirror 块级编辑器  │  内核 API(block.get/update)      │
└───────────────▲───────────────────────────────────▲──────────┘
                │ block-icon/快捷键 触发            │ block.updateBlock 写回
┌───────────────┴───────────────────────────────────┴──────────┐
│                插件核心层（bridge / controller）              │
│   · Dialog 生命周期管理    · 双向同步协调器（DBSC）          │
│   · 防抖调度器            · 类型降级路由                     │
└───────────────▲───────────────────────────▲─────────────────┘
                │ 提取 mermaid 文本           │ onGraphChange(newCode)
┌───────────────┴───────────────────────────┴─────────────────┐
│                 类型适配器层（Adapter Registry）            │
│   flowChartAdapter(正式) │ sequence/class/gantt(降级) │ 可扩展   │
└───────────────▲─────────────────────────────────────────────┘
                │ init(mermaidCode, onGraphChange)
┌───────────────┴─────────────────────────────────────────────┐
│               可视化渲染后端（render layer）                  │
│             Visimer(mermaid-wysiwyg, CST)                    │
│       （后端可替换：VueFlow 桥接，见 §8 兼容设计）            │
└─────────────────────────────────────────────────────────────┘
```

**各层职责**：

- **Host 层**：思源提供块级菜单、Dialog 组件与读写 API，插件不侵入其渲染引擎。
- **插件核心层**：负责触发、Dialog 生命周期、双向同步闭环、防抖、类型路由。
- **类型适配器层**：把"图类型"与"渲染后端"解耦，是本次兼容升级设计的落点。
- **渲染后端层**：Visimer 承担画布绘制与 CST 文本改写。

---

## 5. 触发与挂载（交互设计）

### 5.1 触发方式（二选一，均可注册）

- **block-icon**：在 Mermaid 代码块的 `block-icon` 区域注入操作按钮，用户鼠标悬停点击进入编辑。
- **快捷键**：光标置于 Mermaid 代码块内，触发注册的快捷键（如 `Alt+M`）。

> 不重写思源自带渲染：仅监听思源的块级菜单事件，不改动 ProseMirror 的 DOM 结构。

### 5.2 画布挂载（选型：Dialog）

点击触发后，弹出**思源标准 Dialog 组件**承装画布：

- **优点**：交互清晰、专注编辑；实现复杂度最低；不污染编辑区布局。
- **收益**：规避 Shadow DOM 内嵌带来的样式继承、事件冒泡、块布局重排等一系列问题。

---

## 6. 双向同步核心数据流

### 6.1 正向流（读取 + 渲染）

1. 用户点击"可视化编辑"，插件取得当前选中的 Mermaid **blockId**。
2. 调用 `window.siYuan.api.block.getBlockMarkdown({ id: blockId })` 获取该块源码。
3. 从返回的 markdown 中**剥离 ` ```mermaid ` 围栏**，得到纯 Mermaid 文本 `mermaidCode`。
4. 插件核心层将 `mermaidCode` 交给当前图类型对应的 **Adapter → 渲染后端（Visimer）** 初始化画布。

### 6.2 反向流（写回）

Visimer 在用户拖拽/改字后，通过 CST 直接修改原文本字符串，触发 `onGraphChange(newCode)` 回调：

```
onGraphChange(newCode)
  → 防抖调度（见 §7）
  → 核心层组装完整代码块内容：```mermaid\n${newCode}\n```
  → window.siYuan.api.block.updateBlock({ id, data: fence })
```

### 6.3 闭环时序（示意）

```text
block-icon 触发
      │
      ▼
getBlockMarkdown ─→ 提取纯文本 ─→ 适配器 ─→ Visimer.init(文本)
      │                                                  │
      │                                   用户拖拽/改字（CST 改文本）
      │                                                  ▼
      └────────────────────────────────  onGraphChange(newCode)
                                                │
                                       debounce 500ms / onDragEnd
                                                ▼
                                    updateBlock(用围栏包回 newCode)
                                                │
                                      思源代码块文本无损更新（笔记内容存续）
```

---

## 7. 关键性能设计：防抖（Debounce）策略

> **核心红线**：拖拽/连线会触发极高频坐标更新，**绝不允许在 onDrag 同步调用 `updateBlock`**，否则将导致思源内核索引锁死、卡顿甚至崩溃。

### 7.1 双通道分级同步

| 变更类型 | 同步策略 | 说明 |
| --- | --- | --- |
| **文字修改** | 即时 或 onBlur | 文本变化频率低，可立即同步；推荐 onBlur 触发一次 |
| **节点拖拽 / 连线** | `debounce(fn, 500)` 或 onDragEnd | 高频坐标事件，仅在停顿 500ms 或鼠标松开时写回一次 |

### 7.2 实现要点

- 采用**可取消的防抖**：拖拽持续期间反复重置定时器，仅在停止操作后真正落盘。
- 用 **onDragEnd / onMouseUp** 时强制 flush 一次，兜底防抖误吞最后一次变更。
- 写回期间对画布**加锁（isSyncing）**，防止读回旧文本覆盖新编辑（竞态）。

---

## 8. 兼容升级设计（Visimer 迭代 → 多图类型）

这是本设计的延伸重点：**未来 Visimer 迭代升级支持更多图类型时，插件应能平滑接入、且不破坏已上线的 flowchart 核心逻辑。**

### 8.1 适配器注册机制

抽象一个统一的**图类型适配器接口**，每种图类型一个实现，通过注册表统一管理：

```ts
// 统一适配器接口（伪代码示意）
interface DiagramAdapter {
  type: string;                          // "flowchart" | "sequence" | ...
  supportLevel: 'full' | 'readonly';     // 能力等级：全编辑 / 仅预览
  init(code: string, opts: AdapterOptions): void;
  destroy(): void;
}

// 注册表：对应加类型即插即用，核心同步逻辑零改动
registry.register(FlowChartAdapter);     // 底层走 Visimer CST
registry.register(SequenceAdapter);      // Visimer 支持后，把 supportLevel 升为 full
registry.register(new ReadOnlyAdapter()); // 兜底：未支持类型一律降级只读
```

### 8.2 能力探测与降级路由

打开可视化编辑时，插件**先探测当前图类型能力**，再决定走哪条路径：

```text
解析 mermaid 首行 判定图类型
        │
        ├─ registry 中有 full 适配器 → 进入可视化编辑（Visimer 画布）
        ├─ registry 中有 readonly 适配器 → 弹只读预览（渲染 mermaid，禁止改）
        └─ 未知类型 → 兜底只读，提示"该图类型暂不支持可视化编辑"
```

- 当 Visimer **升级新增类型**：只需在 registry **新增一个支持 full 的适配器**，无需改动双向同步/防抖/写回逻辑。
- 当某类型在 Visimer **暴露 Bug**：可临时把该类型记录为 readonly，规避故障，不影响其它类型。

### 8.3 渲染后端可替换（兜底）

若后续某类型在 Visimer 持续不稳，可**只为该类型的适配器内部**切换到方案B（VueFlow 桥接）作为渲染后端，双向绑定核心保持共用。图类型与渲染后端**一对一解耦**，实现"按需混搭"。

---

## 9. 边界、错误处理与降级

| 场景 | 处理策略 |
| --- | --- |
| Mermaid 语法错误 | 捕获解析异常，保留原文本不写回，画布提示错误而非回写坏数据 |
| 非支持图类型 | 降级为只读预览 + 提示 |
| 写回竞态 | `isSyncing` 锁 + 编辑版本号比对，丢弃过期回调 |
| 插件卸载 | 数据已存于代码块原生文本，**零损坏**，满足存续兼容约束 |

---

## 10. 目录结构与构建约定（建议）

```
plugin/
├── plugin.json            # 思源插件清单（名称/入口/事件注册）
├── vite.config.ts         # 打包（esbuild 亦可）
├── src/
│   ├── index.ts           # 插件入口：注册 block-icon / 快捷键 / Dialog
│   ├── controller/        # 双向同步协调器、防抖、路由
│   ├── adapters/          # 图类型适配器 + registry
│   ├── render/            # Visimer 后端封装（画布初始化/销毁）
│   └── types/             # 适配器接口、常量、能力枚举
└── dist/                  # 产物，供思源安装
```

---

## 11. 成功标准（验收可测）

1. 在 flowchart 上拖拽/改字后，思源代码块源码**准确、无损**更新；卸载插件后内容不受损。
2. 拖拽高频事件**不卡死**思源内核（防抖生效，可观测无连续落盘风暴）。
3. 修改代码块文本后，重新打开画布能正确反映（正向流成立）。
4. 模拟新增一个"full 图类型适配器"，**无需改动**双向同步/防抖/写回逻辑即可接入（验证 §8 兼容设计）。

---

## 12. 下一步建议

本阶段为**设计文档**交付，暂不进入编码。建议按以下顺序推进：

1. 初始化插件工程骨架（plugin.json + vite + 入口注册）。
2. 实现最小闭环：block-icon → Dialog → getBlockMarkdown → Visimer → updateBlock → 防抖写回，跑通 flowchart。
3. 补全适配器注册表与降级路由。
4. 挂接验收用例（§11 四项）。